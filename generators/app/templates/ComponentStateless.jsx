import React from 'react';
import PropTypes from 'prop-types';

const <%= name %> = (props) => {
  return ();
}

<%= name %>.defaultProps = {
  exProp: ''
};

<%= name %>.propTypes = {
  exProp: PropTypes.string
};

export default <%= name %>;
